import React from "react";
import SectionHeader from "../global/SectionHeader";

const Testimonials = () => {
  return (
    <section className="container">
      <SectionHeader title="Recent Feedback From Our Clients" />
    </section>
  );
};

export default Testimonials;
